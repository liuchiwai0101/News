# 2026-07-16

- 执行 AI HOT 晨报自动化（automation-1784089722231）：成功生成 2026-07-16 日报并推送 GitHub Pages。
  - 今日 AI HOT：5 版塊 23 条；NewMobileLife「限時情報王」：20 条（NML 监测 1189 篇，本日 1 新帖但 build 时 NEW 标为 0）；舊聞分页 19 条。
  - GH Pages：https://liuchiwai0101.github.io/news/ 已更新（commit 51cd3a2, 26 文件）。
  - 微信：WeCom/SCRM 均 disconnected，未推送；已存 `ai-daily-2026-07-16.html`。
  - 注意：scrape_nml 在 build 前跑会把 nml_seen 刷成全量，使 NEW 标记恒为 0；下次若要让 NEW 有意义需调整顺序或基线文件。

## 表頭/彈窗 UX 重設計 (09:43)
- 用戶要求：① Old News tab 移到表頭右側不新開一行；② 表頭時間重複→減少位置；③ 移除卡片「原文」連結；④ 展開全文後重設退出模式（手機右上 X 難按）；⑤ 手機點方塊彈出、再點直接退出；⑥ 刪除「讀全文」。
- `build_dashboard.py` 改動：
  - hero 重排：`.hero-inner` 改 flex（hero-main 左 / `#tabs` 右，inline 不另起一行）；stats 移到 hero 內。`#tabs` 由原本 hero 下方的獨立 `.tabs` 列移入 hero，樣式改深底白字 pill（`.tab.on`=白底深字）。tab 文案精簡為「最新/舊聞」+計數。
  - 去重：h1 只顯示日期（`DATA.reportHuman`），kicker 改「AI HOT 晨報」，副標保留收錄區間；移除原本 h1 的「 AI 日报」重複字樣。
  - 卡片 `.actions` 移除 `讀全文`(a.read) 與 `原文`(a.src) 連結，僅留「展開全文」按鈕；`card.dataset.sname` 存來源名供彈窗署名。
  - Reader 彈窗改「點按退出」：移除右上 `✕` 與底部「原文 ↗」連結；`.reader-bar` 改標題+「來源：X」文字署名（無連結）；底部 `.reader-foot`「輕觸任意處關閉」為關閉熱區。JS：`document` 點擊委派——點 reader 內任意處即 `closeReader()`，點卡片（非 a/button）或「展開全文」即 `openReader()`；Esc 仍可關。解決手機 X 難按問題。
- 重新生成並 push main（commit「ux: inline Old News tab in header, tap-to-close reader...」）。JS 通過 `node --check`。線上 https://liuchiwai0101.github.io/news/ 同步。

## 表頭對齊修正 (10:04)
- 用戶反饋：表頭（hero）感覺「移位」，標題與右側 tabs 不在同一視覺基線。
- 調整 `build_dashboard.py`：
  - 將 `.hero-inner` 改為只放 `h1` 標題與 `.tabs`，並用 `align-items:center` 垂直置中對齊；加上 `margin-left:auto` 讓 tabs 在桌面靠右、手機換行時仍靠右。
  - 把 kicker 移到獨立 `.hero-top`，副標與 stats 移到 `.hero-meta`，避免舊設計中 subtitle 把左側撐高、導致 tabs 被不自然推擠。
  - `.hero-meta` 與 `.hero-top` 都設 `max-width:1080px; margin:0 auto`，確保統計區塊與標題列左右對齊，不會在大螢幕偏左。
  - 統一 hero、nav、wrap 的水平 padding 為 18px，消除各區塊左右邊界不一致造成的「微移位」。
  - 手機版微調：縮小 hero padding、標題與 tab 尺寸，換行時 tab 仍靠右。
- 重新生成並 push main（commit「fix: header alignment — split title/tabs/meta, center tabs with title」）。JS 通過 `node --check`。線上 https://liuchiwai0101.github.io/news/ 同步。

## 展開方塊改為「表頭寬度 + 磨砂填空」 (10:23)
- 用戶要求：點擊方塊放大只需跟表頭寬度（1080px），其餘部分磨砂填空。
- `build_dashboard.py` 改動：
  - `.reader` 由原本全屏白底改為**磨砂背景遮罩**：`background:rgba(15,23,42,.42); backdrop-filter:blur(10px)`，flex 置中。
  - 新增 `.reader-panel`：`width:100%; max-width:1080px`（= 表頭寬度）白底、圓角 16px、陰影；內含 bar/scroll/foot。HTML 把原 reader 內容包進 `.reader-panel`。
  - 點擊退出邏輯不變（document 委派：`reader.contains(e.target)` 即 `closeReader()`），因 panel 是 reader 子元素，點 panel 或磨砂區都會關閉。
- 重新生成並 push main（commit「ux: reader now header-width panel over frosted backdrop」）。JS 通過 `node --check`。線上同步。

## 英文卡刪除 + 中文網站補充 (10:30)
- 用戶要求：內容為英文的方塊直接刪除；根據表頭資料（版塊標題）搜尋其他中文網站補充。
- `build_dashboard.py` 改動：
  - `process_item` 回傳後標 `it["embedded"]`（body 非 notice 即中文已嵌入）；每個 item 記 `_sec_label`（來自 section label，NML=「限時情報王」）。
  - 構建後**過濾刪除**所有 `embedded=False` 的卡片（英文/社交/無法擷取），`flat`/`sections` 同步過濾；寫出 `.workbuddy/tmp/dropped_en.json`（{版塊: [被刪標題]}）。
  - 讀取 `.workbuddy/tmp/supplement.json`（{版塊標題: [文章dict]}），把中文補充插入對應版塊（標 `it["supplement"]=True`），再跑 process_item 嵌入；標「補充」藍徽章（`.cover-supp`）。只補「有被刪項目」的版塊。
  - 重新計算 TOTAL / stats。
- 新增 `fetch_supplement.py`：讀 `supplement_urls.json`({版塊:[url]})，擷取+只留中文（is_chinese）+派生標題/摘要，輸出 `supplement.json`。
- 本輪實作：先用 WebSearch 以 5 個被刪版塊（模型发布/更新、产品发布/更新、行业动态、论文研究、技巧与观点）為關鍵字搜中文網站，取得 URL→`supplement_urls.json`→`fetch_supplement.py`→`supplement.json`（16 篇：3/3/3/3/4），重建後總 38 篇（NML 20 + AI HOT 18）。
- 自動化 `automation-1784089722231` 已更新 prompt：加入 step4 中文補充（讀 dropped_en.json→WebSearch→寫 supplement_urls.json→fetch_supplement.py→寫 supplement.json）+ step5 第二次構建合併；確保每日自動「刪英文+補中文」。
- 重新生成並 push main（commit「feat: drop English cards, supplement with Chinese-site articles」）。線上同步。

## 前端網站匯入（內容整理 + 客觀化） (11:1x)
- 用戶要求：內容整理（添加分段、總結、提升客觀性），資料搜索包含 掘金/InfoQ中文/開源中國/SegmentFault/w3ctech，匯總到現有 tab（不另開新 tab）。
- 新增 `.workbuddy/tmp/frontend.json`：{產品發布/更新、行業動態、技巧與觀點} 各含文章 dict（title/summary/sourceName/sourceUrl）；summary 為我撰寫的客觀繁體 1–2 句總結。共 14 篇（掘金3、InfoQ3、開源中國2、SegmentFault4、w3ctech2），依主題分配版塊。
- `build_dashboard.py` 改動：
  - 抽出通用 `_merge_extra(json_path, flag_key)` 取代原本 inline 的 supplement 合併；同時合併 `supplement.json` 與 `frontend.json`。每篇解析 OG/Openverse 封面圖、擷取全文（簡體轉繁），合併後再次過濾 `embedded=False` 的非中文卡。
  - 新增「前端」紫色徽章（`.cover-front`，flag `item.frontend`）。
  - Reader 彈窗頂端新增「重點整理」區塊（`.reader-summary`）：`openReader` 時把 `card.dataset.summary` 渲染於全文上方；card 加 `dataset.summary`。
  - 容錯：擷取全文失敗（代碼重的 CSS/JS 教學、週刊等被誤判非中文）且有 summary 時，以 summary 作為閱讀器內文並保留卡片，而非直接丟棄（原 Container Queries、前端週刊即此路徑）。
- 本輪 14 篇全數納入，總條數 52（限時情報王20 + 模型3 + 產品9 + 行業7 + 論文3 + 技巧10）。
- 自動化 `automation-1784089722231` 加入 step 4b「前端網站每日補充」：每日 WebSearch 5 站、撰寫客觀繁體總結、寫 `frontend.json`，最終構建自動合併。
- 重新生成（node --check 通過）並 push main（commit「AI HOT daily 2026-07-16 · 前端網站補充 + 重點整理/分段」）。線上 https://liuchiwai0101.github.io/news/ 確認：前端徽章、各站來源、總 52 條均出現。

## 修復亂碼程式碼、強化圖片與重點整理 (11:27)
- 用戶反饋：內容有「亂碼 程序」、不夠 readable、需要照片、分段與更精簡完整的總結。
- `article_pipeline.py` 改動：
  - 改用 trafilatura `output_format="markdown"` 擷取，保留程式碼圍欄 ``` 並解析為獨立 `code` block；`render_body` 將 code block 輸出為 `<pre><code>`（深色區塊、保留換行與縮排），避免程式碼與中文說明被空格連接成亂碼。
  - 新增 `_is_junk` 過濾：移除文末「掃碼關注微信公眾號」等廣告/關注提示。
  - 新增 `_split_long_para`：在句號處拆分過長中文段落，提升可讀性。
  - 新增 `extract_first_image`：從原文 HTML 抓第一張夠大的內容圖（跳過 avatar/logo/QR/微信/招聘廣告/小於 240px 圖），作為封面圖候選。
  - 語言檢測 `is_chinese` 改以 prose 段落為準，排除 code block 中的英文，避免 code-heavy 中文教學（CSS Container Queries、React 效能、Next.js）被誤判為英文而丟棄。
- `build_dashboard.py` 改動：
  - 圖片優先順序：OG image → 內文圖 → Openverse；新增 `fetch_page_images` / `fetch_content_image` 減少重複請求。
  - 擴充 Openverse 關鍵字（Vue/React/CSS/JS/Vite/瀏覽器/跨端/UI/效能等），提升前端文章配圖相關性。
  - Reader「重點整理」區塊強化：保留 1 句 summary 並新增 `keypoints` 條列（3–4 點），以 `<ul>` 呈現；`frontend.json` 14 篇全部補上 `keypoints`。
  - Reader CSS 增加 `<pre><code>`、`<img>`、`<h3>` 樣式，讓程式碼與圖片在彈窗內清晰可讀。
- `frontend.json`：summary 改寫為更精簡的 1 句核心總結，每篇新增 3–4 條 keypoints。
- 自動化 `automation-1784089722231` 更新 step 4b：要求前端補充文章提供 `summary` + `keypoints`。
- 清除 article_cache.json 與 img_cache.json 後重新生成（45+ `<pre><code>` 區塊），JS 通過 `node --check`；部署到 GitHub Pages（commit 092c6a7）。線上確認：前端徽章、原題圖/Openverse 圖片、總 52 條、無 QR/招聘廣告封面。

## 自動任務重複檢查 (12:00)
- 列出全部 automations：共 2 個。
  - `automation-1784089722231`「AI HOT 日报 + 限時情报王 · 每日 7:00 推送」(DAILY 07:00)：完整日報主流程（API→scrape_nml 基線→build_dashboard→補充→GitHub Pages+WeCom）。
  - `automation-1784099008467`「流動日報 限時情报王 · 每日更新監測」(HOURLY INTERVAL=2)：專門、每 2 小時監測限時情报王新文→產 nml-daily.html→推 WeCom。
- 結論：非重複/非新舊版關係，而是互補（每日完整日報 + 高頻限時情报王提醒）；兩者共用 scrape_nml.py 與 nml_seen.json/nml_snapshot.json 狀態，無衝突。用戶確認「保留兩個」，未刪除任何一個。

## 修復手機卡片重疊 (12:10)
- 用戶反饋：手機下方塊之間有重疊。
- 問題根因：手機單欄時仍套用 JS masonry 的 `gridRowEnd: span N`，文字摘要過長導致計算高度不夠，下一張卡片覆蓋上去。
- `build_dashboard.py` 改動：
  - 手機 media query（max-width:640px）加 `grid-auto-rows:auto;` 與 `.card { grid-row-end:auto !important; }`，直接停用單欄 masonry，讓卡片自然堆疊。
  - 手機 `.card p` 加 `-webkit-line-clamp:5`，限制摘要最多 5 行，避免卡片過長。
  - `layoutMasonry()` 內偵測 `window.innerWidth <= 640` 時把 `gridRowEnd` 設為 `auto`；桌機仍維持 masonry 效果。
  - 增加 `window.addEventListener('load', layoutMasonry)` 與 `document.fonts.ready.then(layoutMasonry)`，圖片/字體載入後重新計算，減少高度漂移。
- 重新生成（node --check 通過）並部署到 GitHub Pages（commit e21b56a）。線上網址：https://liuchiwai0101.github.io/news/

## 移除外部來源標籤與來源標題，統一內容寬度 (12:26)
- 用戶反饋：不需要「外部來源」表述與「NewMobileLife 流動日報」來源標題；內容寬度應跟隨 header。
- 改動：
  - 移除 `group-divider`「外部來源 · 每日監測」分隔線。
  - 移除 section head 的「NewMobileLife 流動日報」ext-chip 徽章。
  - 移除「來自 NewMobileLife（流動日報）· 置頂-限時情報王分類，每日監測更新」section-sub 副標。
  - 移除 `.section--ext` 特殊背景/邊框/內距，讓限時情報王版塊與其他版塊一致，內容寬度對齊 header（1080px）。
  - 保留 footer 最簡版權歸屬說明。
- 重新生成並確認 index.html 中「外部來源」「每日監測更新」「section--ext」「ext-chip」「section-sub」均為 0；JS 通過 `node --check`；部署到 GitHub Pages（commit 342a957）。線上 CDN 快取可能需要數分鐘刷新。

## 移除「展開全文」按鈕，直接點方塊開啟 (12:42)
- 用戶要求：不需要「展開全文」按鈕，直接點擊方塊即可開啟全文彈窗。
- `build_dashboard.py` 改動：
  - `makeCard` 移除 `.actions` 裡的「展開全文」`<button class="expand">`；卡片 HTML 只留 cover / h3 / p。
  - 移除 `.card .read`、`.actions`、`.expand`、`.card .src` 等已不再使用的 CSS。
  - `.card` 加 `cursor:pointer` 提示可點擊。
  - 點擊委派簡化：移除 `.expand` 偵測，改為 `e.target.closest('.card') && !e.target.closest('a')` → `openReader(card)`，直接點卡片任一處即開彈窗。
- 重新生成（52 篇、6 版塊）並部署（commit 0e6c78e）。線上 https://liuchiwai0101.github.io/news/ 同步。

## 客製 favicon (14:19)
- 用戶要求：生成有個性的瀏覽器 tab 圖示。
- 新建 `favicon.svg`（64×64）：圓角方塊 + 紫(#7c3aed)→靛(#4f46e5)→天藍(#0ea5e9) 漸層，中央白色四角 spark（代表 AI HOT / 限時情报王）+ 靛色圓心。與站點 accent 配色一致。
- `build_dashboard.py` <head> 加入 `<link rel="icon" type="image/svg+xml" href="favicon.svg">`。
- 檔案同時置於 `ghpages/favicon.svg` 與 `build_site/favicon.svg`（每次重新生成不遺失）。
- 重新生成並部署（commit cd0dd64）。線上 tab 圖示已更新（CDN 快取可能需數分鐘/硬刷新生效）。

## 政治正確 / 合規檢查 (14:31)
- 用戶要求：檢查相關設計是否潛在政治不正確。
- favicon / 視覺設計：✅ 無問題。spark 為抽象圖案，無國旗、地圖、領土邊界；配色為品牌 accent。
- 內容合規：⚠️ 發現問題（非 favicon，而是文章內文）。站點聚合 NewMobileLife（台灣媒體）內容，多篇將 台灣/香港 列為與國家並列的地區：
  - 線上文章標題/內文「Apple Sports App 加入世界盃專屬功能 擴展至香港、台灣等 90 個地區」
  - nml_snapshot 摘要「中國、香港、台灣、馬來西亞及新加坡發售」「香港、台灣時間今夜…」等
- 依規範：台港澳屬中國一部分，應書寫為「中國台灣 / 中國香港 / 中國澳門」，不得視為獨立國家/地區。
- 建議：在 `article_pipeline.py` / `build_dashboard.py` 增加正規化步驟，顯示前將 台灣/臺灣→中國台灣、香港→中國香港、澳門→中國澳門（保留來源署名）；並可加「機器翻譯自第三方媒體」免責聲明。待用戶確認後實作並重新部署。

- 新增「熱門優惠」獨立 group（Price.com.hk 網購 top-20 優惠）。實作：`scrape_price.py`（live scraper + snapshot 回退，結構仿 scrape_nml.py）、`price_snapshot.json`（20 條真實優惠，含 title/price/orig/url）、`build_dashboard.py` 新增 `load_price_section()` 插入 `sections_out`（ci=6 琥珀色 accent，kind="deals"）。修正：deals 卡無全文，須豁免「embedded」過濾（line 452 的刪非中文卡邏輯會把 deals 清掉）；新增 `makeDealCard()` + 優惠 reader（價格/折扣 + 前往連結）。注意 price.com.hk sandbox 回 403（anti-bot），live scrape 失效，靠 snapshot 提供內容（與 NML 同模式）。已部署 commit 8a05a78，驗證 20 件優惠、價格齊全、領土正規化 0 違規（2 處自動轉 中國香港）。JS 通過 node --check。
- **GitHub Pages CDN 快取滯後坑**：推送新 commit（8a05a78）後，live 仍回傳舊版 index.html。解決：在 `build_dashboard.py` 注入 `<meta name="build" content="...">` 與 favicon `?v=` 時間戳，每次構建內容唯一 → 重新 commit/push（bca6820）強制 Pages 重建並刷新。經驗：比對 raw URL vs live URL 後強制重建。
- **熱門優惠卡片 redesign**：用戶回報「看不到名稱、圖片、原價、優惠價」。舊設計把價格大字放在封面上，遮擋標題；原價/折扣與標題重疊。重新設計：封面改為 140px 圖片區（產品圖或購物袋 placeholder），「優惠」徽章與編號置於封面角落；內文顯示標題（兩行截斷）、優惠價（大橙色字）、原價（刪除線）、折扣徽章（省 xx%）、來源。Price.com.hk 主站有 Cloudflare anti-bot / 產品圖不可抓，故暫以白色購物袋 SVG 為 placeholder。已部署 commit fd324a6，live 驗證 131KB、deal-src / cover-icon / dp-cur 均存在，20 件優惠。

- **熱門優惠加產品圖（Openverse 圖片回退）**：用戶要求「加圖片，若網站抽不到就網上搜近似圖」。Price.com.hk 產品圖被 anti-bot 擋，改用 **Openverse API**（aggregates Wikimedia/Flickr/CC，hotlinkable）作為圖片來源。`scrape_price.py` 新增 `resolve_deal_images()`：查詢順序 = 精確型號 → 品牌+型號 → 品牌+品類（_CAT_MAP，CN/EN 關鍵字對應，如 耳機→headphones、焗爐→oven、legion go→handheld、lift→beauty device）→ 純品類；結果寫回 `price_snapshot.json["deals"][i]["image"]`，建置時 `load_price_section()` 讀取並快取回 snapshot。`makeDealCard` 封面改為「icon 在後 + img 在前(z-index:1)」，img 加 `onerror="this.remove()"` 失敗時露出 icon；reader 加 `.reader-deal-img` 與 `hideDealImg()` onerror 隱藏。20/20 全部解析到圖（部分為近似品類圖，因新機型 Openverse 無精確圖）。注意：sandbox 對圖床的 HEAD/GET 驗證不穩定（偶發 410/超時），故**不依賴驗證、改靠 onerror 兜底**，避免誤刪可用圖。已部署 commit 0a04c11，live 驗證 20 件優惠全部含 img、onerror/hideDealImg 存在。

## 合規修正實作 (14:44)
- 用戶確認「按照建議修改」，已實作並部署（commit d3125c7）。
- `build_dashboard.py`：新增 `normalize_territory(text)`（台灣/臺灣→中國台灣、香港→中國香港、澳門→中國澳門）。
  - 對 `flat`（卡片標題/summary/keypoints，與 sections_out 共享引用）與 `OLD_ITEMS` 全面套用。
  - 對 `ARTICLES`（reader 內文，含 Old News）套用；並對 external(NML) 且無免責聲明的 body 前置「內容機器翻譯自第三方媒體（來源名），僅供參考，以原文為準」notice。
  - 末尾對 `build_site/articles/*.html` 全量再正規化（含上版遺留的 stale 孤兒頁，如 apple-sports-app…html）。
  - 曾嘗試 `shutil.rmtree` 清空 articles/ 再建，但沙箱 safe-delete 阻擋（windows-sandbox-recycle-bin-unavailable），故改為「全量正規化」而非刪除。
- `article_pipeline.py`：同步新增 `normalize_territory`，於 `build_article_page` 標題、`process_item` 的 body/page 寫檔前套用；並將預設 notice 補上「內容機器翻譯自第三方媒體」。
- 驗證（線上 ghpages）：index.html 與 articles.js raw(非中國前綴) = 0；99 個 articles/*.html raw = 0、已正規化 4 篇；articles.js 含「第三方媒體」免責。✅ 全部合規。
- 17:49 修復「熱門優惠」中 Sony REON POCKET PRO Plus 錯配人物照片問題：
  - 根本原因：Openverse 圖片搜尋被限流，且舊版邏輯會 fallback 到不相關照片。
  - 已改為 `cooler` 類別 icon（穿戴式冷暖調溫裝置專用），並強制 build timestamp 刷新 CDN/瀏覽器快取。
  - 部署 commit b4bf518；請用戶 Ctrl/Cmd+Shift+R 強制刷新。
  - 備註：Price.com.hk 與 Openverse 仍受限，真實產品照需等後者恢復或手動提供圖片 URL。

## 熱門優惠 deal 圖片一致性修復 (17:4x)
- 用戶投訴：熱門優惠卡片圖片「不完全是產品照片」（舊邏輯取 Openverse 第一筆 → 出現荷蘭燭台配 Dell 顯示器、Evercade 配 Lenovo Legion Go 等錯配）。
- `scrape_price.py` resolve_deal_images 重寫為「相關性閘門」：
  - `_score()` 只接受「品牌+類別」(brand 3 + category 2) 或「型號+（品牌或類別）」的候選；跨品牌/跨類別一律拒絕（競品懲罰 -12）。
  - 修 bug：`\b(?:19|20)\d{2}\b` 只刪真年份，原 `(?\d{4})?` 會刪掉型號數字（WF-1000XM6→WF- XM6）。
  - 新增 `_model_tokens()` 做型號比對；`_ov_search` 加查詢快取 + 429 僅退避 1 次(5s) 避免 80s 卡死。
  - `resolve_run2.py` 每張即時存檔 + 背景跑（12s 間隔避開 rate limit）。
- `build_dashboard.py`：deal 卡無圖時改顯示準確「類別 icon」(CATEGORY_ICONS：phone/monitor/headphones/oven/tv/laptop/...)，reader 圖壞掉 `dealImgFallback` 也退類別 icon；`detectCat()` 對中英文標題分類。
- 現狀：Price.com.hk 全站 403（含圖片 CDN），Openverse 目前被 sandbox rate-limit（查詢回 0）。故熱門優惠暫以「準確類別 icon」呈現（無錯配照片），已 build+部署（git 顯示 working tree clean，即線上已是 icon 版，用戶看到的是舊 CDN 快取）。
- 待辦：rate limit 恢復後重跑 resolver 即可自動補上「品牌+類別」匹配的真正產品照；或請用戶提供 Price.com.hk 直接圖片網址 / 解除封鎖。
