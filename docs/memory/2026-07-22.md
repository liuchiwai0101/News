# 2026-07-22

## 網站版面改為 E 緊湊卡片牆（17:14）
- 將線上站由監控台表格改為 E 方案：4 欄緊湊卡片網格（響應式 3/2/1 欄）、8px 間距、細色條 + 標題 + meta，移除大封面與 summary、KPI 條、masonry。
- 同步改 `.workbuddy/tmp/build_dashboard.py` 模板（f-string `{{ }}` 轉義，py_compile 通過）以免日後重建還原。
- 發佈 commit db47a70 → main；站點 https://liuchiwai0101.github.io/news/

## AI HOT 晨報自動化（automation-1784089722231）每日執行
- 拉取 2026-07-22 日報（5 版塊 / 31 條，NEW 均 0）。NML 限時情報王 20 條 + 熱門優惠 20 條。
- 修復 `fetch_supplement.py`：trafilatura `extract_blocks` 現返回 `[{type,text}]` 字典列表，舊碼當 str 處理導致 TypeError；改為取出 `b.get('text')` 後再 `convert_zh_blocks` / `is_chinese` / `title_of`。
- 最終構建 68 條 / 7 版塊；中文補充 14、前端補充 12。
- GitHub Pages 推送：xcopy 在 Git Bash 須用 `xcopy build_site ghpages /E /I /Y`（正斜線、來源無結尾反斜線），否則會把 build_site 當子目錄複製成 cyclic copy。commit d35587b 已上 main。
- 微信：WeCom / SCRM 連接器皆未連，未推送；儀表盤 HTML 已存 build_site/index.html。

## 修復網站空白頁（同日下午）
- 根因：`build_dashboard.py` 模板 `onerror` 用 `\'`（Python 折成裸 `'`），產出 inline script 引號錯誤 → 整段 JS 解析失敗、卡片不渲染。兩處：line 1162 `this.style.display='none'`、line 1301 `dealImgFallback(this,'' + dcat + '')`。
- 修復：模板改 `&quot;none&quot;` 與 `dealImgFallback(this,\\'' + dcat + '\\')`；同步修 build_site/index.html + xcopy 至 ghpages，commit c2fbfd7 推送。
- 驗證：`node --check` 通過 + DOM shim 跑出 7 版塊 / 68 卡片。建議每次 build 後對 index.html 跑一次 `node --check` 內聯 script。

## 「沒有 7月16後的資料」診斷（同日下午）
- 用 jsdom 實際渲染 build_site/index.html，逐版塊印出卡片日期：AI HOT 5 個版塊（模型发布/更新、产品发布/更新、行业动态、论文研究、技巧与观点）全部顯示 **7月22日 周三**，內容是最新的；只有 **限時情報王(NML) 卡在 7月16日**（NewMobileLife 該專欄自 7/16 後未出新文，重跑 scraper 仍 NEW=0），且自動化只抓「單日」日報，故 7/17–7/21 完全不在資料裡。
- 用戶看到「沒有 7月16後的資料」是因為 `build_dashboard.py` 把 NML/熱門優惠 `insert(0)/insert(1)` 到最前面，頁面一開就先看到卡在 7/16 的 NML。
- 修復：改為 `append`（放到最後），讓 7/22 AI HOT 新聞置頂；同時更符合原始 spec（限時情報王 第6欄）。已重新 build（build3）待推送。
- 待辦（視用戶需求）：若要填補 7/17–7/21 空缺，需改 `fetch_daily.py` 抓取最近 N 天日報並合併去重（較重，需再次長時間 rebuild）。

## 限時情報王 欄固定置頂（13:56）
- 新增 `.workbuddy/tmp/pin_nml_top.py`：把 `const DATA.sections` 中 限時情報王 移到陣列首位，並依新順序重建 nav-inner（anchor=#sec-{i}、dot 顏色沿用各 section 的 ci，與 body 欄色條一致）。
- 已套用 build_site/index.html、ghpages/index.html；驗證首 section=限時情報王、nav anchors #sec-0..6 連續對齊、JSON 合法。頁面一開即看到 限時情報王（含 32 篇 NEW）。

## 發佈到 GitHub Pages（14:01）
- 同步 build_site → ghpages（`cp -r build_site/. ghpages/`，diff 驗證乾淨），git add/commit/push（commit 3ce8cee）。
- 6 files changed（+99/-13），含新建 nml-daily.html。已 push 至 origin/main（c2fbfd7..3ce8cee）。
- 標準 GH Pages 網址：https://liuchiwai0101.github.io/news/ （無 CNAME）。部署後首欄確認為 限時情報王。
- 注意：push token（x-access-token）存於 ghpages/.git/config，勿外洩。

## 合併兩個自動任務為 1 個（14:0x）
- 用戶要求：每天重複「抽取最新文章 → 整合 index → 發佈」，並把「限時情报王監測(1784099008467, 每2h)」與「AI HOT 晨報(1784089722231, 每日7:00)」合併為單一每日任務。
- 已刪除 1784099008467；將 1784089722231 改名為「AI HOT 日报 + 限時情报王 整合 · 每日 7:00 更新發佈」，prompt 改為合併流程（fetch_daily → scrape_nml → build_dashboard → 補充 → 二構建 → nml-daily 報表 → pin → 發佈 → WeChat）。
- 關鍵技術修改（避免基準時序誤報 + 永久置頂）：
  - `scrape_nml.py`：跑完後額外寫 `.workbuddy/tmp/nml_new.json`（本輪真正新增 url 集合），在更新 nml_seen.json 之前產生 → 作為「本輪 NEW」唯一權威來源。
  - `build_dashboard.py`：① load_nml_section 改為優先讀 nml_snapshot.json（雙分類 限時情报王+限時免費情報），fallback 才 live scrape；② NEW 標記改用 nml_new.json（不再用 nml_seen 比對，避免全站誤報）；③ NML section 由 append(最後) 改 insert(0, 最前)，NML_MAX 20→40。
  - `build_nml_report.py`：NEW 標記改讀 nml_new.json（移除舊的 nml_seen/nml_transition 比對）。
  - `pin_nml_top.py`：保留作冪等安全網（build 已置頂則不變動）。
- 實際跑通整條 pipeline 並發佈：fetch_daily 成功(5版塊/31條)、build_dashboard 成功（限時情报王 第一欄 40 篇、總 88 條 7 版塊）、nml-daily.html 重建、pin 冪等、cp→ghpages→push（commit 49da329）。今日 NEW=0（基準已含全站 2767）。
- 結論：未來每日 7:00 單一自動化即完成「抽取+整合+發佈」；限時情报王 永久置頂且正確標 NEW，不會誤報。

## 改回「只抽取限時情報王」單一分類（14:29）
- 用戶要求：只抽取限時情報王最新文章（撤銷之前的「雙分類」決定，不再納入限時免費情報）。
- `scrape_nml.py`：CATEGORIES 改回單一 限時情報王 URL，並加 `BASE = CATEGORIES[0]` 別名供 build_dashboard fallback 使用；docstring 改單分類。
- `build_dashboard.py`：NML_MAX 註解、load_nml_section docstring/內聯註解改回「限時情報王 only」。
- 重跑整條 pipeline 發佈：scrape_nml → 1189 篇（限時情報王單分類）、NEW=0（舊基準 2767 含這 1189，無誤報）；build_dashboard → 限時情報王 第一欄 40 篇、總 83 條 7 版塊；nml-daily.html 重建；pin 冪等 → cp→ghpages→push（commit 03c9ea7）。
- 自動化 1784089722231 prompt 同步改回「限時情报王 單一分類」，並加註：限時情報王是置頂精選、更新慢，NEW 多為 0 屬正常，勿誤報。rrule 仍 FREQ=DAILY;BYHOUR=7。

## 對齊 App 限時情報王 → 新聞熱話 + 限時免費情報（14:50）
- 用戶出示 App 截圖指出 7-22 當日 7+ 篇文章（AppleCare+、TypeFlow、Claude Code 桌面版、OpenAI 入侵 Hugging Face、Gemini 3.6…）未出現在我們的 nml-daily.html 與 index。
- 排查：網頁「置頂 - 限時情報王」是靜態精選（更新慢），但 App 的「限時情報王 → 最新文章」是從 `新聞熱話` + `限時免費情報` 兩個分類聚合而來。
- `scrape_nml.py` CATEGORIES 改為 [新聞熱話, 限時免費情報]；docstring/BASE 註解同步。
- 過渡處理：備份舊基準 1189 → nml_seen_prev.json；run scraper 得 5582 篇（與舊基準重疊 224）；手動把 nml_seen.json 改成「1189 ∪ 5582 = 6547」聯集基準；nml_new.json 改成「≥07-15 且不在舊基準 = 97 篇」小集合（避免 5358 篇洪水誤報）。
- 重跑 build_dashboard → 限時情報王 第一欄 40 篇、全 NEW 標記（包含截圖全部文章）；nml-daily.html 重建（總 5582 / 顯示 40 / NEW 40）；pin 冪等；cp→ghpages→push（commit 583499e）。
- 自動化 1784089722231 prompt 同步註明「對齊 App 最新文章 feed」與「網頁置頂限時情報王不是來源」，並加註變更 CATEGORIES 需手動合併基準避免洪水。
