# 2026-07-23 工作記錄

## AI HOT 日報 + 限時情报王 自動化（07:00 定時任務）
- 完成每日整合：daily(3 版塊/24) + NML 雙分類 scrape + 中文補充 + 前端補充 + 置頂 + GitHub Pages 部署。
- **關鍵優化**：NEW 標識改為「new-this-run 且 date==今日(北京)」日期閘（build_dashboard.py / build_nml_report.py）。結果 限時情报王 40 項中 14 NEW，其餘版塊 0 NEW。
- 最終儀表盤 80 條 / 5 版塊；nml-daily.html 14 NEW。已 push（0055941 + 88e410f 刷新 nml-daily）。
- 網址 https://liuchiwai0101.github.io/news/ ；NML 報告 /nml-daily.html。
- 坑：build_site/nml-daily.html 為舊版（40 NEW），已補 commit 刷新。論文類實抓易失敗（arxiv SSL / toutiao 無區塊）→ 改手寫 supplement 條目 + summary fallback 較穩。
- WeCom/SCRM disconnected → 未推送，存 ai-daily-2026-07-23.html + nml-daily.html。

## 修正：每個 tab 的 # 序號從 #1 重新開始
- 用戶要求單一 tab 與每個版面都從 #1 編號。根因：build_dashboard.py 用 module-level `globalIdx` 跨 panel 累加，導致單一 tab 顯示繼續的 global 數（如 #81+）。
- 修復：改為 panel 內局部 `secIdx`（每個 buildPanel 重置為 0）。重跑 build → pin 置頂 → push commit `109fbf8`。驗證 globalIdx=0 / secIdx=3、兩檔 IDENTICAL、`node --check` 過。

## 修正：# 號碼在圖片卡片上不可見
- 用戶截圖顯示有封面圖的卡片缺少 # 號碼（僅無圖的第一張顯示 #1）。
- 根因：`.card .idx` 原用 `background:rgba(0,0,0,.32)` + `backdrop-filter:blur(2px)` + `z-index:2`，在照片背景上對比度不足/模糊失效導致不可見。
- 修復：改為 `background:rgba(0,0,0,.58)`（實心不透明黑底）+ 移除 backdrop-filter + `z-index:4`（全 cover 最高層）。重跑 build → pin → push commit `f14d8dd`。

## 修正：# 號碼與 補充/前端 標籤重疊
- 用戶截圖回報「號碼跟標籤有重疊」。根因：`.idx`(left:12px) 與 `.cover-supp`/`.cover-front`(left:14px) 同擠左上角。
- 修復：補充/前端 標籤右移到 `#` 號碼右側（`top:10px; left:50px`）。重跑 build → pin → push commit `4aecb7d`。

## 功能：非中文內容自動翻譯顯示在內容裡
- 用戶要求「如果內容不是中文 直接翻譯顯示在內容裡面」。
- 實作：build_dashboard.py 加 key-less Google `gtx` 翻譯（→zh-TW，帶快取）。原本「非中文卡整張刪除」改為「保留+翻譯」：
  - 文章卡 summary 中文<20% → 翻譯 title+summary、加 `譯` 標記（卡片標題內）、存原標題/摘要、建合成 reader body。
  - 原本被誤刪的 22 張「中文標題+英文來源頁萃取失敗」卡 → 現保留並以中文 feed summary 作為 reader body。
  - 熱門優惠 deal 為價格元資料（非外文正文），依設計排除翻譯。
- 結果：重跑 build → **102 條 / 5 版塊**（限時情报王 40、产品发布/更新 13、行业动态 12、技巧与观点 17、熱門優惠 20）。TRANSLATED=0（今日無真正外文文章卡；22 張為中文，功能待命但已接線）。
- 部署：pin → `cp -r build_site/. ghpages/` → push commit `7bbe602`。ghpages 與 build_site IDENTICAL。
