# 2026-07-15

## AI HOT 日报 HTML 仪表盘
- 用 aihot skill 的 `daily` 接口拉取 2026-07-15 日报（当日已生成，无需回退；回退逻辑在生成脚本中已注释保留）。
- 关键数据事实：`daily` 端点**不含 per-item 时间戳**，只有 daily 级 `generatedAt/windowStart/windowEnd`（UTC）。时间统一在生成期转成北京时间人话格式，HTML 内零 ISO 字符串。
- 构建方式：curl(带浏览器 UA，否则 403) → 重定向写临时 JSON → Python 内联生成单文件 HTML（数据嵌入为 JS const，客户端渲染）。沙箱内绝对路径不可写/读，必须用相对 cwd 路径；Windows 上 Python 写文件是 `\r\n` 换行。
- 产物：`C:\Users\vincentliu\Desktop\AI Test\ai-daily-2026-07-15.html`（单文件、内联 CSS/JS、无外部资源、响应式卡片网格、全局连续编号 1–25、五版块统计、锚点导航）。
- 校验：Node + DOM stub 模拟渲染，确认 25 张卡片、首张 #1、链接带 `target="_blank" rel="noopener noreferrer"`、摘要截断 ≤60 字。

## 设计可读性修订 (12:24)
- 改为更冷静的深靛蓝 hero 渐变；Hero 统计数字统一白色（仅用 c0–c4 作顶部色条/圆点强调），提升对比度。
- 文字层级：标题 #0f172a、正文 #3a4255、次要 #6b7689；链接/序号徽标用更深靛蓝 #2f44c4（AA 对比），卡片摘要加至 14.5px、行高 1.7。
- 版块标题加下划线分隔；卡片圆角/阴影更柔和；补充 `a:focus-visible` 无障碍焦点环。

## 每日 7:00 推送微信自动化 (12:27)
- 创建 recurring 自动化 `automation-1784089722231`（ACTIVE，RRULE FREQ=DAILY;BYHOUR=7;BYMINUTE=0，cwd=工作区）。每天 7:00 拉取 AI HOT 日报→运行 build_dashboard.py→推送微信。
- 把 build_dashboard.py 改为**日期无关**：SRC=`.workbuddy/tmp/daily_today.json`，OUT=`ai-daily-{DATE}.html`，便于每日复用。
- 连接器限制：无个人微信 API；仅有「企业微信 (WeCom)」连接器（当前 disconnected）。自动化优先用 WeCom 发送，未连接则保存文件并提示用户去连接器页连接 WeCom/SCRM。需提醒用户实际连接后才能真送达。

## 发布到网站 (12:29)
- 用户问能否免费发布该页。结论：单文件、无外部依赖、已含数据源署名与原文链接，可自由发布。
- 已 staging：`publish/index.html`（由 ai-daily-2026-07-15.html 复制），并用 `workbuddy_cloudstudio_deploy` 部署到 CloudStudio 沙箱，分享链接 `https://17910e31e71c40f2a665ed5418aa5412.app.codebuddy.work`（公开）。注意：该页聚合 AI HOT 内容，发布时保留署名与原文链接即可。

## 固定鏈接 + 自動更新決策 (12:44)
- 用户选定「固定鏈接 + 內容自動更新」。但環境限制：CloudStudio 每次部署開新沙箱→鏈接會變（不適合）；本機 git 未配置 user/email 與憑證、gh 未安裝→無法直接推 GitHub。
- 結論：固定鏈接必須靠用戶掌控的穩定託管。待用戶提供：(A) GitHub 倉庫+推送權限（連接 GitHub 連接器或給 PAT/SSH），或 (B) 自有域名/伺服器部署方式（SFTP/SSH 或對象存儲+自定義域名）。
- 下一步：用戶提供託管資訊後，把每日 7:00 自動化加上「生成→覆寫 index.html→推送/上傳」步驟，URL 固定。publish/index.html 已備好可推。

## GitHub Pages 固定鏈接（已完成 12:5x）
- 用戶選 A：GitHub Pages 固定鏈接 + 內容自動更新。倉庫 `https://github.com/liuchiwai0101/news`（空倉，用戶提供）。
- 憑證：用戶提供 PAT（repo 全權限）。僅用於本次 clone/push 與 Pages API，未寫入本專案源碼或 memory。
- 做法：clone 到工作區 `ghpages/`；`ai-daily-2026-07-15.html` 複製為 `ghpages/index.html`；commit & push 到 main（exit 0）。
- API `POST /repos/liuchiwai0101/news/pages` 開啟 Pages（branch=main, path=/）HTTP 201，`public:true`、`https_enforced:true`。
- **固定網址**：`https://liuchiwai0101.github.io/news/`（已驗證 HTTP 200，標題正確）。
- 推送憑證目前嵌在 `ghpages/.git/config` 的 remote URL（masked `https://***@github.com/...`），供自動化每日 push。安全提示：該 PAT 有 repo 全權限，建議日後降權/改 deploy key 或撤銷。
- 自動化 `automation-1784089722231` 已更新：每日 7:00 生成→更新 `ghpages/index.html`→git pull/commit/push→微信推送→回報。工作區須保留 `ghpages/` 目錄。

## 行動端（手機）介面優化 (13:00)
- 用戶要求優化手機瀏覽。原僅 `@media(max-width:520px)`（單欄+stat 兩欄）。
- 新增完整行動版 CSS（build_dashboard.py）：`@media(max-width:640px)` + `@media(max-width:380px)`。
- 改動：body 字號 16px（防 iOS 縮放、中文更清晰）；Hero 字級/內距收縮；stats 兩欄且「總條數」佔滿整行；導航改**單行橫向滑動**（不換行、隱藏滾動條、`scroll-margin-top:56px` 修正錨點被遮）；卡片單列、內距收縮、`閱讀原文`變**整寬大按鈕**（更好點按）；超窄屏 ≤380px 進一步收字級。
- 加 `theme-color`(#1b266b) 與 `viewport-fit=cover`，並在 hero/nav/wrap/foot 用 `env(safe-area-inset-*)` 處理劉海安全區；`html` 加 `text-size-adjust:100%`。
- 已重新生成並同步 `publish/index.html` 與 `ghpages/index.html`，push 到 main（commit "移动端优化"，+64/-9）。線上 https://liuchiwai0101.github.io/news/ 同步更新。

## 頂部欄位緊湊化 (14:16)
- 用戶要求「頂部欄位減少佔用位置」。做法：把 Hero 的 6 個統計**大卡片**改為**一行內聯小 pill**（帶 c0–c4 色點，總條數加強為 `共 N 條`），Hero 內距 42/32→28/22、標題 clamp 42→34、副標縮小；手機版 hero padding 24/18、stats margin-top 12、pill 字級 12.5px。
- JS 渲染 stat 改為 `<span class="dot">+<span class="lab">+<span class="num">`，去掉 border-top 色條。舊 `flex:1 1 124px` 區塊樣式已移除。
- 已重新生成並同步 publish/ghpages/index.html，push 到 main（commit「顶部栏位紧凑化」+24/-24）。線上網址不變。

## 卡片升級：封面圖 + 加長摘要 + 可變尺寸 (14:21)
- 用戶要求：方塊摘要加長、加圖片、可變方塊大小、協調整頁分佈。
- 關鍵發現：AI HOT `daily` 接口 item **無任何圖片欄位**（僅 title/summary/sourceUrl/sourceName/permalink/attribution）。故「圖片」用每版塊漸變封面＋內聯 SVG 圖示（代替縮圖），保持單檔、無外部依賴。已向用戶說明此取捨，並提供「改抓原文 OG 圖」的替代路（需外部依賴）。
- 摘要：原本被砍到 ≤60 字，實際 AI HOT 摘要中位 265、最長 321 字 → 現改為**顯示完整摘要**（不再截斷）。
- 可變尺寸：JS masonry（`grid-auto-rows:8px` + 依 `getBoundingClientRect().height` 算 `gridRowEnd: span N`），保留左→右閱讀順序；每版塊首張 `.featured`（桌面跨 2 欄、封面更高、標題更大）。resize 與 `document.fonts.ready` 後重算。
- 新增 CSS：`:root` 加 `--g0..--g4` 五版塊漸變；`.card .cover/.cover-ico/.cover-src/.cover-date/.body`。手機版 featured 收為 1 欄、封面收小。
- 校驗（render_test.js 加 querySelectorAll/getBoundingClientRect stub）：25 卡、5 featured、25 masonry span 套用、首張 #1、連結屬性正確、封面存在、摘要完整。push main（commit「封面图+加长摘要+可变尺寸masonry」+74/-32）。

## 標題收緊 + 主題配圖 (14:29)
- 標題：`-webkit-line-clamp:2` 兩行截斷、字級 16.5→15px、行高 1.5→1.4，featured 20→18px。減少標題佔位。
- 用戶選擇「上網找類似照片貼」而非 AI 生圖（省積分）。實作：build 時用 **Openverse API**（`api.openverse.org/v1/images/?q=&license_type=commercial`，免 key、CC 可商用、縮圖走其自家 CDN 可熱連結）按主題關鍵字抓圖。
- 關鍵字映射：`TERM_Q`（title 含 芯片/機器人/數據/代碼/醫療… → 對應英文 query）+ `SECTION_Q`（版塊兜底）。同關鍵字用 page offset 取不同圖避免重複。結果快取 `.workbuddy/tmp/img_cache.json`。
- 卡片封面：有圖時 `background-image:linear-gradient(暗色遮罩),url(thumb)` cover 置中，隱藏 SVG 圖示，右上角加 `图：creator · LICENSE` 授權標註；抓不到圖則兜底回漸變+圖示。footer 加 Openverse CC 署名+「配圖為示意圖」聲明。
- 本次 25/25 全抓到圖。校驗：idx 1–25 連續、25 卡皆有 photo cover。push main（commit「主题配图(Openverse CC)+标题收紧」）。
- ⚠️ 注意：配圖為外部熱連結（單檔不再完全自包含）；每日自動化會每天重抓 25 張（免費 API，已加 0.25s 節流+快取）。若 Openverse 某日不可用會自動兜底漸變。

## 配圖策略改為「優先原文 OG 圖」(14:43)
- 用戶要求：抓每條**原文自己的題圖（OG image）**，原文**沒有照片**才去 Openverse 搜外部圖。
- 實作（build_dashboard.py）：新增 `fetch_og_image(page_url)` —— 用瀏覽器 UA + `Accept-Encoding:identity` 抓原文頁（timeout 8s、讀前 1.8MB），regex 解析 `og:image`/`og:image:url`/`og:image:secure_url`/`twitter:image`/`twitter:image:src`，`urljoin` 轉絕對網址；失敗/無圖則回退 `fetch_image`（Openverse）。結果按 page_url 快取進 `img_cache.json`。
- 渲染：封面改用 `<img class="cover-img" src=og onerror="this.remove()">` + `.cover-overlay` 暗色遮罩；onerror 時移除 img 露出底層漸變，不開天窗。caption：OG 圖標「原题图」，Openverse 圖標「图：creator · LICENSE」。footer 改稱「優先原文題圖（版權歸原媒體），無圖才用 Openverse 示意圖」。
- 本次 25/25 解析成功（多數來自原文 OG）。push main（commit「优先原文OG题图+Openverse兜底」）。

## Hero 去重（解決「header 內容重負」）(14:53)
- 用戶指 Hero 內容重複/過重。根因：Hero 的 5 個版塊統計 pill（各版塊條數）與下方**導航欄的 5 版塊+條數完全重複**。
- 做法：Hero 只保留「總條數」pill（去重版塊統計），版塊統計交由導航欄（本就有）。JS 移除 Hero 的 `DATA.sections.forEach` 那段。
- 注意：此舉偏離最初需求#4「Hero 展示五版塊統計」，但符合用戶現階段「減輕 header」要求；若想復原可把該 forEach 加回。push main（commit「Hero精简(去重版块统计,仅留总条数)」+1/-8）。

## 流動日報 限時情報王 爬蟲 + 每日監測 (15:0x)
- 用戶要求「重點抓取這個網站所有內容更新」：https://www.newmobilelife.com/category/featured/置頂-限時情報王/
- 網站結構：WordPress 分類存檔（流動日報 / NewMobileLife），`<article>` 區塊 + 分頁 `/page/N/`。
- 自建爬蟲 `.workbuddy/tmp/scrape_nml.py`（urllib，瀏覽器 UA，含重試/節流）：
  - 擷取欄位：title（`<h2/h3><a>`）、url、date（`cs-meta-date` 或 URL 路徑 `/YYYY/MM/DD/`）、excerpt（`cs-entry__excerpt`，截 200 字）、thumb（首張 `<img>`）。
  - 走分頁直到無新 URL；全站共 **1188 篇**（2017-07-18 → 2026-07-14），欄位完整度 0 缺失。
  - 與基準 `nml_seen.json` 比對輸出「相對上次新增幾篇」；首跑已建基準（1188），故下次只報新文。
- 報表生成 `.workbuddy/tmp/build_nml_report.py` → 工作區 `nml-daily.html`（單檔自包含，青綠 hero、卡片網格含縮圖/日期/NEW 徽章/摘要/原文連結）。
- 每日監測自動化 `automation-1784099008467`（ACTIVE，RRULE `FREQ=DAILY;BYHOUR=8;BYMINUTE=0`，cwd=工作區）：每日跑爬蟲→有新增則重生 nml-daily.html 並嘗試 WeCom/SCRM 推送→回報新增篇數+最新 3 篇；無新增則僅回報「今日無新文章」。異常時保留舊基準不誤報。
- 待用戶決定：是否把此站內容併入 AI HOT 儀表盤（第 6 版塊）、或獨立發到 GitHub Pages 子路徑、或僅 WeChat 推送。

## 限時情報王 併入 AI HOT 儀表盤（方案 A）(15:09)
- 用戶回覆「a」→ 選擇**方案 A：把 NewMobileLife「限時情報王」作為 AI HOT 儀表盤第 6 版塊**。
- 改 `build_dashboard.py`：
  - 頂部 `importlib` 載入 `scrape_nml.py` 的 `fetch/parse_articles/BASE`（避免重寫爬蟲邏輯）；新增 `NML_SNAP/NML_SEEN/NML_MAX=20`。
  - 新增 `load_nml_section()`：即時抓分類**前 2 頁**（最新 ~40 篇）去重後取前 20；與 `nml_seen.json` 基準比對標 `isNew`；即時抓取失敗則回退 `nml_snapshot.json`。
  - 第 6 版塊 label=`限時情報王`，sourceName=`NewMobileLife 流動日報`，img 用原文 thumb（`imgKind=og`），`date` 用**真實發布日期**（short_date_iso 轉 `M月D日 周X`，不再用 cardDate）。
  - CSS 加 `--c5`/`--g5`（青藍 #0e7490）、`.dot-5`、`.cover-new`（NEW 紅徽章）；JS `ICONS` 加第 6 個手機圖示；卡片封面日期改用 `item.date||DATA.cardDate`；當 `isNew` 時封面右上顯示 NEW 徽章、否則 OG 圖顯「原题图」。
  - 全局編號 #1–45 連續（AI HOT 25 + NML 20）；footer 加「第 6 欄內容聚合自 NewMobileLife」聲明。
- 重新生成 `ai-daily-2026-07-15.html`：**6 版塊 / 45 卡**（4/6/6/2/7/20），NML 20/20 圖解析成功、20 篇皆「已見」→ NEW=0（基準含全 1188 篇，無誤報）；已 push main（commit「AI HOT 日报 + 限時情報王(第6栏)」）。
- 更新每日自動化 `automation-1784089722231`（重命名為「AI HOT 日报 + 限時情報王 · 每日 7:00 推送」）：步驟 2 先跑 `scrape_nml.py` 刷新基準、步驟 3 跑 `build_dashboard.py` 自動含第 6 欄、步驟 6 回報含限時情報王條數與 NEW 數。線上 https://liuchiwai0101.github.io/news/ 已同步。
- 備註：獨立 `nml-daily.html` 自動化 `automation-1784099008467`（8am）仍保留，作為該站專屬視圖，與第 6 欄互不衝突。
- 安全提醒（待用戶處理）：`ghpages/.git/config` remote 仍明文含 `ghp_yHgi...` PAT（repo 級），建議改為 deploy key 或降權 token。

## 限時情報王 改為「儀表盤內獨立分組」(15:38)
- 用戶回覆「儀表盤内分组」：保留在合併儀表盤，但把網站新聞做成更明顯的獨立分組。
- 實作（build_dashboard.py）：
  - 資料層：AI HOT 各版塊加 `"kind":"aihot"`；限時情報王加 `"kind":"external"` + `"subtitle":"來自 NewMobileLife（流動日報）· 置頂-限時情報王分類，每日監測更新"`。
  - CSS 新增 `.group-divider`（外部來源分隔線）、`.section--ext`（淡青分組面板+圓角邊框）、`.section-head--ext`（標題轉青色）、`.ext-chip`（NewMobileLife 流動日報 來源徽章）、`.section-sub`（分組副標）；手機版 `.section--ext` 收內距。
  - JS：渲染時 `isExt` 判斷——外部分組前插入 `.group-divider`、套用 `section--ext`/`section-head--ext`、標頭加 `.ext-chip`、如有 subtitle 加 `.section-sub`。
- 重新生成併 push（commit「限時情報王 改为仪表盘内独立分组」+39/-5）。線上 https://liuchiwai0101.github.io/news/ 已同步。分組視覺上與前 5 個 AI HOT 版塊明確區隔。

## 流動日報 調整為第 1 欄 (15:58)
- 用戶：「place 流動日報 in the first option」→ 把限時情報王分組移到儀表盤最前面（第 1 欄）。
- 實作：NML 區塊改 `sections_out.insert(0, ...)`；為避免顏色/圖示隨位置錯亂，引入與位置無關的 `ci`（color index）：AI HOT 5 版塊 ci=0..4，限時情報王 ci=5（青色+電話圖示）。`stats` 元組加 `ci`；nav `dot-{ci}`；JS 用 `const ri = sec.ci ?? si` 取代 `--c`/`--g`/`ICONS[si]`。
- 結果：欄位順序 = 限時情報王(ci5)→模型(0)→產品(1)→行業(2)→論文(3)→技巧(4)；nav 點序 `5,0,1,2,3,4`；AI HOT 顏色保持不變。重新生成併 push（commit「流動日報...调整为仪表盘第1栏」）。線上已同步。

## 全文擷取改為「僅中文、略過翻譯」(17:5x)
- 用戶：「extract the data or news only in chinese 繁體 or 簡體, skip the translate process.」
- 改 `.workbuddy/tmp/article_pipeline.py`：
  - 移除翻譯路徑：`to_traditional_blocks` → `convert_zh_blocks`，只對**已是中文**的 block 做 OpenCC 簡→繁（字體轉換，非翻譯）；非中文 block 原樣保留但**不會被嵌入**（因為整篇判定非中文時直接略過）。
  - `process_item`：擷取後用 `is_chinese(combined)` 判定**原文是否為中文**；是→轉繁並嵌入本地頁+`articles.js`；否→跳過整個翻譯步驟，改顯「原文為非中文內容，已略去翻譯步驟，未提供本地譯文」提示並連回原文。社交貼文(x.com/weixin)/無法擷取仍走原 fallback。cache 新增 `lang` 欄位(zh/social/nofull/other)以支援增量重建。
  - `build_article_page` 提示語改為「原文轉錄（繁體中文，簡體已自動轉繁）」；footer「翻譯」→「轉錄」。
- `build_dashboard.py`：inline 展開 fallback 文案改「原文非中文，未提供本地譯文，請見『原文』連結」。
- 清空 `article_cache.json` 重新擷取（舊 cache 含機器翻譯的英文 body，須按原文語言重判）。
- 結果：**45 篇中 21 篇中文全文嵌入**（NML 20 + ithome 等中文源），**24 篇略過**（英文源/社交/無法擷取），全程零 gtranslate 呼叫。已重新生成 `build_site/` 並 push main（commit「chore: Chinese-only article extraction (skip translation)」）。線上 https://liuchiwai0101.github.io/news/ 已同步。

## 展開面板文字貼邊修正 (17:59)
- 用戶截圖反饋：卡片展開全文後，文章內容文字貼邊（與卡片邊緣太近）。
- 修正 `.workbuddy/tmp/build_dashboard.py` 的 `.apanel` / `.apanel-in` CSS：
  - `.apanel` 加 `padding:14px 16px; background:#f8fafc; border-radius:12px;` 形成內凹面板、避免文字貼邊。
  - `.apanel-in h3:first-child` / `p:first-child` 移除上 margin，避免首段/首標題與面板頂部出現多餘空白。
- 已重新生成並 push main（commit「fix: add padding to expanded article panel」）。線上網址同步更新。

## 卡片 UI 再精簡 (18:02)
- 用戶：「remove unnecessary data source header, and make the photo smaller.」
- `build_dashboard.py`：移除卡片封面上的來源徽章 `cover-src`（與「原文 ↗」連結重複，視為不必要）；同步刪除 JS 中的 `cover-src` span 與 CSS `.card .cover-src` 規則（誤改一次產生重複，已清掉）。
- 縮小封面圖：桌面 cover 132→96px、featured 196→132px；手機 cover 116→96px、featured 150→120px；對應 padding 微調。
- 已重新生成並 push main（commit「ui: remove card source badge, shrink cover photo」）。線上網址同步更新。

## 舊聞 Old News 分頁 + 跨日歸檔 (18:0x)
- 用戶：「每日更新以上版面，超過5天後的舊news 搬入一個新的 group tab 叫 Old News.」
- `build_dashboard.py` 新增跨日歸檔：
  - 每筆 item 記 `isoDate`（AI HOT=報表日；NML=真實發布日，新增 `parse_iso` 解析 `YYYY/MM/DD` 等）。新增 `ci/kind/isoDate` 進 rec 與 NML item。
  - 歸檔檔 `.workbuddy/tmp/article_archive.json`（key=sourceUrl），每日合併當日 `flat`；年齡 `(北京今日 - isoDate).days > 5` 且不在當日 flat 者 → `OLD_ITEMS`，按日期倒序。
  - Old News 卡片沿用 `makeCard`（抽出共用函數），以原 `ci` 上色；獨立 `#view-old` 容器 + 頂部 `.tabs` 切換（最新動態 / 舊聞 Old News），各自計數徽章，無舊聞時隱藏該 tab。內文展開改 `document` 層級委派，兩個 view 都可用。
  - 舊聞 body：從 `article_cache.json` 取（當日曾擷取），缺則 fallback 提示。歸檔保留約 90 天（`_MAX_AGE=90`）後自動清理，避免無限膨脹。
  - `payload` 加 `"old": OLD_ITEMS`；`articles.js` 改為 if 外統一寫入（含舊聞 body）。修復 `utcnow` 棄用警告 → `datetime.now(timezone.utc)+8h`。
- 驗證：手動 seed 一則 isoDate=10天前的舊聞，重建後 `DATA.old` 長度=1、JS 通過 `node --check`；清理 seed 後重建乾淨歸檔（45 筆當日）。push main（commit「feat: Old News tab + cross-day archive」）。線上同步。
- **修正每日自動化 `automation-1784089722231`**：原 step3 用系統 python（缺 trafilatura/opencc 會 ImportError）、step4 只複製舊產物 `ai-daily-{日期}.html`（現已改輸出到 `build_site/` 且需連帶 `articles.js`/`articles/`）。已更新 prompt：step3 改用 venv `envs/default/Scripts/python`；step4 複製整個 `build_site`（`index.html`+`articles.js`+`articles/`）到 `ghpages/` 再 git pull/commit/push。確保每日自動出報含舊聞分頁。

## 全文改為「全頁 Reader 彈窗」(18:21)
- 用戶手機回饋：方塊展開會**重疊**。根因：inline `.apanel` 與 masonry 網格高度重算衝突，手機上版面錯位。
- 改為 **全頁 Reader 彈窗**（modal overlay）：`position:fixed;inset:0;z-index:1000`，頂部深藍 bar（標題+✕ 關閉）、中間可捲動 `.reader-scroll`、底部 `.reader-actions`（原文 ↗ 連結）。
- 互動：① 點「展開全文」按鈕或**點卡片任意處（連結/按鈕除外）**即開全頁；② 同一時間僅一個彈窗，切換卡片時舊卡自動恢復原大小（因不再 inline 展開）；③ ✕、點背景、Esc 關閉並恢復 body 捲動。
- 刪除 `makeCard` 的 `.apanel` div 與 `.apanel*` CSS；卡片改以 `card.dataset.aid/src` 存 articleId 與 sourceUrl；原 inline expand handler 換成 `openReader/closeReader` + 文件層委派（最新/舊聞兩 view 通用）。
- 重新生成並 push main（commit「fix: full-page reader modal (fix mobile card overlap)」）。JS 通過 `node --check`。線上 https://liuchiwai0101.github.io/news/ 同步。
