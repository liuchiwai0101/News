# 2026-08-13 工作日誌

## 舊聞分組導航（手動跟進需求）
- 用戶要求：舊聞分頁也應像最新版面一樣提供 group 導航。
- 改 `build_dashboard.py`：
  - `view-old` 加 `<nav class="nav nav-old"><div class="nav-inner-old">`；`renderOld` 動態填入「全部」+ 各板塊連結（帶即時條數）。
  - 舊面板 class 改 `section old-panel` 並加 `data-cat=板塊`；點擊板塊僅顯示該面板。
  - 主版面 nav 處理器 scoping 至 `#view-current`，避免誤傷舊聞面板。
  - CSS：`.nav-inner-old` 共用 `.nav-inner` 樣式。
- 驗證：jsdom 載入 build_site/index.html 模擬點擊 → PASS（全部 684 / 7 板塊篩選正常、互不干擾）。
- 部署：rebuild → cp build_site→ghpages → commit `ef16558` → push `ba899f4..ef16558`。線上網址不變。
- 測試時曾暫時注入 6 筆 test-old.example 舊條目驗證，已自 archive 還原（.bak），無殘留。
