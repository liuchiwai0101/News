# 2026-08-10 工作紀錄

## 每日整合自動化 (automation-1784089722231) 08:00 執行
- 續跑因網路/超時中斷的 8/10 排程：完成 fetch_daily（今日 AI HOT 僅 4 條 stub → 合併 8/08 完整期，得 25 條 / 5 版塊）、scrape_nml（nml_new 合併備份得 19 urls：13@8/10 + 6@8/09）、第一次構建、中文補充、前端補充、第二次構建。
- 第二次構建：**117 條 / 7 版塊** — 限時情报王 40、熱門優惠 20、技巧与观点 17、产品发布/更新 16、行业动态 10、论文研究 9、模型发布/更新 5。supplement 合併 23、frontend 合併 9（含 Nuxt 4.5 / Rspack 2.1 / Vite 6.2 / Vue 3.6 RC / Webpack→Rspack 實戰等真實中文前端文）。
- nml-daily.html：total 5746 / shown 40 / **new_vs_baseline 13**（13 篇標 2026-08-10）。
- 品質：`_inline_check_today.js` 通過（1 block, ok:1, bad:0）；裸引號 bug 0 命中。
- 限時情报王 pin 置頂確認（build_site + ghpages 皆 ok）。

## 關鍵修復：ghpages git push 卡死
- 症狀：push 懸停 19 分鐘、remote tip 不變。讀/ls-remote 正常，唯 write 卡死。
- 根因：`ghpages` 倉庫 `credential.helper=manager`（Git Credential Manager）在非互動 shell 下 push 會卡。
- 修復：`git -C ghpages config --local credential.helper ""`（token 已內嵌 URL）。禁用後 push 2 秒完成（af39846..98323bd）。已寫入專案 MEMORY.md 作為常態慣例。
- 附帶：`protocol.version=0` + `http.version=HTTP/1.1` 可繞過偶發 `expected flush after ref listing` v2 協商錯誤。

## 程式小改
- `build_dashboard.py` 的 `_merge_extra` except 分支加 curated-summary 回退（代碼為主的中文前端文經 skip-nonzh 拋錯時仍可藉 summary 留卡，不漏報）。
