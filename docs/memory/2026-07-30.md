# 2026-07-30 工作日誌

## 新增：在線訪客即時計數
- 用戶要求「加入 在線訪客 數量」。採用 herenow.js 免費後端（per-page 分頁統計，非全域），自寫輕量中文客戶端，複用同一後端。
- 已寫入兩個模板：`build_dashboard.py`（主儀表盤 index.html，hero 右上角徽章）+ `build_nml_report.py`（nml-daily.html，與 .pill 並排徽章）。
- 後端 `https://herenow-anhz7w.fly.dev`；每 60s ping + 取 count；標籤「在線訪客 N 人」，含脈動綠點；後端不可達時徽章自動隱藏。
- 已重新 build 兩頁 → `cp -r build_site/. ghpages/` + `cp nml-daily.html ghpages/` → git commit `a48eb5e` 推送。
- 線上驗證：https://liuchiwai0101.github.io/news/ 與 /nml-daily.html 均已含 online-count（GitHub Pages 重建後生效）。

## 調整：在線訪客徽章移至右下角
- 用戶要求「實時在線人數 顯示在 右下角」。將 `.online-badge` 由 hero 內右對齊改為 `position:fixed; right:16px; bottom:16px; z-index:60` 浮動徽章（主頁深藍底、nml 頁青藍底，含陰影）。
- 標籤改為「實時在線 N 人」（原「在線訪客 N 人」）。
- 重新 build 兩頁 → cp→ghpages → git commit `0aa3364` 推送；線上已驗證 position:fixed + 實時在線 標籤生效。

## 調整：在線人數改為「按 IP」統計（自建 Cloudflare Worker）
- 用戶要求「按照 ip 來計算 人數」。純靜態 GitHub Pages 無法自己按 IP 去重，需小後端。
- 評估 kvdb.io：免費 TTL-KV 但寫入需驗證 email、且訪客 IP 會明文暫存第三方 → 捨棄。用戶選擇 **Cloudflare Worker**（免費免信用卡、IP 存自己的 KV 最隱私）。
- 新建 `online-counter/`：`worker.js`（取 CF-Connecting-IP 寫 KV，key=IP/TTL=90s；`/online` 統計 60s 視窗內不同 IP 數）、`wrangler.toml`、`README.md`（一次性部署步驟）。
- 改寫兩模板的在線計數腳本：移除 herenow，改打 `WORKER`（佔位 `https://news-online-ip.<your-subdomain>.workers.dev`），heartbeat 60s / online 30s；失敗自動隱藏徽章。
- 已重新 build 兩頁 → cp→ghpages → git commit `274e8de` 推送。**徽章暫隱藏**，待用戶部署 Worker 並回填真實 subdomain 後才亮起（詳見 `online-counter/README.md`）。

## 調整：在線計數加 herenow 回退（立即可見）
- 用戶反映「not see the online ppl」。原因：Worker 未部署 → fetch 失敗 → 徽章自動隱藏。
- 在兩模板加 **回退**：先試 Worker `/online`；失敗則改打 herenow `/ping`(帶 sessionId)+`/count?page=`，顯示連線數（最小顯示 1）。
- 驗證 end-to-end：模擬 ping 後 `/news/` 回傳 `{"count":1}`，徽章可見。
- 重新 build 兩頁 → cp→ghpages → git commit `ca95dd0` 推送。

## 新增：舊聞（Old News）按原板塊歸類
- 用戶要求「舊聞 也需要按照原來的 板塊歸類」。原本 OLD_ITEMS 全部混在一個 grid、無板塊資訊。
- `build_dashboard.py` 改動：
  1. 歸檔時記錄原板塊：`_url_board` 由 `sections_out` 建立 url→label 映射，寫入 `_archive[_u]["board"]`。
  2. 歷史 backfill：舊 archive 條目無 board，依 `kind` 推斷（external→限時情報王、deals→熱門優惠；aihot 留空→「其他」）。
  3. `renderOld` 改為按 `board` 分組，複用 `panel-head`（色條+標題+條數）+ `grid wall`，板塊順序跟隨 `DATA.sections` 當前順序。
- 效果（本輪 238 舊聞）：限時情報王 149、其他 89。其他=歷史 aihot 無法回溯板塊，隨 90 天 prune 與新條目帶板塊歸檔而逐漸減少。
- 重新 build → cp→ghpages → git commit `6d6670c` 推送；線上確認 board 欄位與分組邏輯已生效。
